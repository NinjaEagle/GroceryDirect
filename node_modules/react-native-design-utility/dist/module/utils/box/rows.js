import get from'lodash.get';import*as React from'react';import Box from'../../components/Box';export var boxRowsUtils=function boxRowsUtils(props){var newChild=props.children;var rows=get(props,'rows');if(rows&&Array.isArray(rows)){newChild=React.Children.map(props.children,function(child,i){if(child.type===Box){return React.cloneElement(child,{f:rows[i]||1});}throw new Error('Each child for a Box with rows need to be a Box');});}return newChild;};
//# sourceMappingURL=rows.js.map