import { ITheme } from '../types/ITheme';
interface IProps {
    theme: ITheme;
    border?: number;
}
export declare const borderUtils: (props: IProps) => {
    borderWidth?: number | undefined;
};
export {};
