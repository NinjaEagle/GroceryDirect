import { ITheme } from '../../types/ITheme';
interface IProps {
    theme: ITheme;
    center?: boolean;
    right?: boolean;
    left?: boolean;
}
export declare const textAlignUtils: (props: IProps) => {
    textAlign?: string | undefined;
};
export {};
