import React from 'react';
interface IProps {
    capitalize?: boolean;
    capitalizeEach?: boolean;
    uppercase?: boolean;
    lowercase?: boolean;
    thousand?: boolean;
    children?: React.ReactNode;
}
export declare const manipulationUtils: (props: IProps) => React.ReactNode;
export {};
