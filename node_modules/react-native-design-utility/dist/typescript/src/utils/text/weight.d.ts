import { ITheme } from '../../types/ITheme';
interface IProps {
    theme: ITheme;
    light?: boolean;
    bold?: boolean;
    normal?: boolean;
    weight?: string;
}
export declare const weightUtils: (props: IProps) => {
    fontWeight?: string | undefined;
};
export {};
