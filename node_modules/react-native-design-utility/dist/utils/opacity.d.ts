import { ITheme } from '../types/ITheme';
import { OpacityType } from '../types/OpacityType';
interface IProps {
    theme: ITheme;
    o?: number | OpacityType | string;
}
export declare const opacityUtils: (props: IProps) => {
    opacity?: number | undefined;
};
export {};
