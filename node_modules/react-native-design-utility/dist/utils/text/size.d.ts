import { ITheme } from '../../types/ITheme';
import { FontSizeType } from '../../types/FontSize';
interface IProps {
    theme: ITheme;
    size?: number | FontSizeType | string;
}
export declare const textSizeUtils: (props: IProps) => {
    fontSize?: number | undefined;
};
export {};
