import { LetterSpacingType } from '../../types/LetterSpacing';
import { ITheme } from '../../types/ITheme';
interface IProps {
    ls?: LetterSpacingType | number | string;
    theme: ITheme;
}
export declare const letterSpacingUtils: (props: IProps) => {
    letterSpacing?: number | undefined;
};
export {};
