import { ITheme } from '../../types/ITheme';
interface IProps {
    color?: string;
    theme: ITheme;
}
export declare const colorUtils: (props: IProps) => {
    color?: string | undefined;
};
export {};
